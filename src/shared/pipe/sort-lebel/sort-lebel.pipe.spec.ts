import { SortLebelPipe } from './sort-lebel.pipe';

describe('SortLebelPipe', () => {
  it('create an instance', () => {
    const pipe = new SortLebelPipe();
    expect(pipe).toBeTruthy();
  });
});
